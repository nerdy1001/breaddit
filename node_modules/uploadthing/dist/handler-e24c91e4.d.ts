import { F as FileRouter } from './types-c0cdeb03.js';

type RouterWithConfig<TRouter extends FileRouter> = {
    router: TRouter;
    config?: {
        callbackUrl?: string;
        uploadthingId?: string;
        uploadthingSecret?: string;
    };
};

export { RouterWithConfig as R };
