import { ScanCommandOptions } from "@upstash/redis/types/pkg/commands/scan.js";
import { Command, CommandOptions } from "@upstash/redis/types/pkg/commands/command.js";
/**
 * @see https://redis.io/commands/zscan
 */
export declare class ZScanCommand extends Command<[
    number,
    (string | number)[]
], [
    number,
    (string | number)[]
]> {
    constructor([key, cursor, opts]: [
        key: string,
        cursor: number,
        opts?: ScanCommandOptions
    ], cmdOpts?: CommandOptions<[
        number,
        (string | number)[]
    ], [
        number,
        (string | number)[]
    ]>);
}
